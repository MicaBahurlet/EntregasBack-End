import { Server } from "./models/server";

const server = new Server(); // creo una variable server del tipo server. Esto es una instancia de la clase Server y se ejecuta el contructor

server.listen(); // listen poruqe al definir la clase cree un metodo listen. 